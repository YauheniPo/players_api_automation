# All values are empty here — override via one of the following (priority high to low):
#
#   1. Environment variables:
#      BASE_URL=... ADMIN_LOGIN=... ADMIN_PASSWORD=... ./gradlew test
#
#   2. JVM system properties (-D flags):
#      ./gradlew test -Dbase.url=... -Dadmin.login=... -Dadmin.password=...
#
#   3. config.local.properties (git-ignored, for local development):
#      src/main/resources/config.local.properties
#
base.url=
admin.login=
admin.password=
